spring:
  application:
    name: api-gateway

  cloud:
    config:
      fail-fast: true
      retry:
        max-interval: 10
        max-attempts: 1000
        initial-interval: 1000
      uri: http://localhost:8888

  config:
    import: "configserver:"