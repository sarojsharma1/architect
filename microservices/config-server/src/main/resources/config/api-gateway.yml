server:
  port: 8021

spring:
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: process-service
              uri: lb://process-service
              predicates:
                - Path=/process/**

            - id: data-service
              uri: lb://data-service
              predicates:
                - Path=/data/**

            - id: auth-server
              uri: lb://auth-server
              predicates:
                - Path=/auth/**
    discovery:
      locator:
        enabled: true
        lower-case-service-id: true